# Constructor 옵션: `onConstructor_ = @__(@JsonCreator(...))`

```java
@AllArgsConstructor(
    onConstructor_ = @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
)
```

## `onConstructor_ = ...`

* Lombok이 **생성자에 추가로 붙일 어노테이션**을 지정하는 옵션
* 즉, *“Lombok이 만든 생성자에 이 어노테이션도 붙여라”* 라는 뜻

---

## `@JsonCreator(mode = JsonCreator.Mode.PROPERTIES)`

* **Jackson 어노테이션**
* JSON → 객체 역직렬화 시
  👉 **생성자를 사용해서 객체를 만들겠다**
* `Mode.PROPERTIES` 의미:

  * 생성자의 **파라미터 이름과 JSON 필드 이름을 매칭**해서 값 주입

```json
{
  "name": "kim",
  "age": 20
}
```

⬇️ Jackson은 아래 생성자를 사용

```java
new User("kim", 20)
```

---

## 🔹 전체 의미 한 줄 요약

> **모든 필드를 받는 생성자를 만들고,
> Jackson이 JSON을 객체로 변환할 때 그 생성자를 사용하도록 지정한다**

---

## 언제 필요한가?

* 기본 생성자(`@NoArgsConstructor`)가 없을 때
* 불변 객체 (`final` 필드 사용)
* 생성자 기반 역직렬화를 명확히 하고 싶을 때

---

## 주의할 점 (중요)

* **컴파일 옵션에 `-parameters`가 없으면**

  * 생성자 파라미터 이름을 Jackson이 모를 수 있음
  * 이 경우 `@JsonProperty("name")`를 각 파라미터에 붙여야 안전함